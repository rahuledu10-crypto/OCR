[build]
builder = "NIXPACKS"

[build.nixpacks]
buildCmd = "pip install emergentintegrations --extra-index-url https://d33sy5i8bnduwe.cloudfront.net/simple/ && pip install -r requirements.txt"

startCmd = "uvicorn server:app --host 0.0.0.0 --port $PORT"
